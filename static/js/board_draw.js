(function(context) {
	if (context.board_draw) {
		return;
	}

	context.board_draw = {
		/**
		 * 게시판 리스트 그리기
		 */
		set_board_list: function() {

		},
		/**
		 * 게시판 view 그리기
		 */
		set_board_view: function() {

		},

	};
})(window);
